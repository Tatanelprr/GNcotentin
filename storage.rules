rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Images des news : lecture publique, upload admin uniquement
    match /news/{newsId}/{fileName} {
      allow read: true;
      allow write: if request.auth != null;
    }
  }
}
